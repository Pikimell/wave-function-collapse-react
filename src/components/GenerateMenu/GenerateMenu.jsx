import style from './GenerateMenu.module.css';
import { useState } from 'react';

const GenerateMenu = ({}) => {
  return <div className={style.container}>GenerateMenu</div>;
};

export default GenerateMenu;
